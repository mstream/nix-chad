export ABBR_SET_EXPANSION_CURSOR=1
export ABBR_SET_LINE_CURSOR=1
# a hack to avoid prezto setting LESSOPEN variable in .zprofile
# the script is incompatible with KSH_ARRAYS option enabled
export LESSOPEN="echo %s"

